define(function(require, exports, module) {

    require('core');

    var $ = window.Zepto,
        UI = $.AMUI;

    var PureBox = function(element, options) {

    };

    PureBox.prototype.init = function() {

    };

    UI.purebox = PureBox;

    module.exports = PureBox;
});
