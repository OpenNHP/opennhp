# NHP-Server cluster 2 resource config (generated by CI)
#
# Identical shape to cluster 1: the server only maps an AuthServiceId to a
# plugin. The actual resource -> AC mapping (which AC guards "demo") lives
# in the plugin's own resource.toml, rendered per-cluster to point at the
# cluster 2 AC. See deploy-demo-v2.yml (server2 plugin render step).
["example"]
PluginPath = "example/example.so"
