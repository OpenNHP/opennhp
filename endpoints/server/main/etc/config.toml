# NHP-Server base config
# field with (-) does not support dynamic update

# PrivateKeyBase64 (-): server private key in base64 format.
# DefaultCipherScheme: 0: curve25519, 1: gmsm.
# MIGRATION: Values changed in v2.x. If upgrading: old 0(gmsm)->new 1, old 1(curve)->new 0.
# ListenIp (-): udp listening address.
# ListenPort (-): udp listening port.
# Hostname (-): server domain name.
# LogLevel: 0: silent, 1: error, 2: info, 3: audit, 4: debug, 5: trace.
# DisableAgentValidation: whether for the server to skip the agent's public key validation.
# AllowPrivateRelaySource: accept private (RFC1918 / loopback / CGNAT)
#   addresses in RelayForwardMsg.SourceAddr. Keep false in production:
#   a public-facing relay should only ever forward public clients, so
#   a private SourceAddr there is a sign of a compromised relay
#   injecting fabricated entries. Set true only in trusted local
#   demos where the relay legitimately sees private addresses
#   (e.g. Docker Desktop's vpnkit gateway).
# ForceOverload: pin the device's Overload flag ON at startup so every
#   agent goes through the cookie-stamped KNK → COK → RKN handshake.
#   Debug/test only — production must rely on the natural connection-
#   count trigger. Leave false except when reproducing or testing
#   cookie-path behavior on a quiet local stack.
PrivateKeyBase64 = "eHdyRHKJy/YZJsResCt5XTAZgtcwvLpSXAiZ8DBc0V4="
DefaultCipherScheme = 0
ListenIp = ""    # empty for ipv4 + ipv6, "0.0.0.0" for ipv4 only
ListenPort = 62206
Hostname = "localhost" # the hostname of NHP-Server
LogLevel = 4
DisableAgentValidation = false
AllowPrivateRelaySource = false
ForceOverload = false

# Tamper-evident security audit ledger (optional, disabled by default).
# When Enabled, security decisions (knock granted/denied over both the UDP
# and HTTP paths, agent registered) are appended as hash-chained JSON lines.
# When on, this is the server's structured audit trail (the log "[Audit]"
# stream is unused). Verify with:
#   nhp-serverd audit verify <FilePath> [--key <SigningKeyBase64>] [--strict]
# IMPORTANT — what the chain does and does not protect against:
#   Without SigningKeyBase64 the chain detects accidental corruption and
#   casual edits, but NOT a determined attacker: anyone who can write the
#   file can recompute the whole SHA-256 chain and produce a file that
#   verifies clean.
#   SigningKeyBase64 binds the chain to a secret, but the key is read from
#   THIS config file, so it does not survive an attacker who has taken over
#   the server (that attacker can read the key and re-sign). What it does
#   defend: an attacker who can write the log but not read this config (a
#   compromised log-shipping account, or offline edits to an archived copy).
#   For host-compromise resistance the key must live off-host in an
#   append-only sink the server can write but not rewrite.
#   One limit remains even with a key: dropping entries from the END of
#   the file leaves a shorter chain that still verifies. A hash chain
#   cannot prove it has not been shortened. To catch truncation/rollback,
#   periodically copy the latest seq+hash somewhere off-host and compare
#   the count / last hash back against it.
# FilePath: ledger path; relative paths resolve against the exe dir.
#   Defaults to audit/audit-ledger.jsonl (its own dir, not logs/, so a
#   logrotate rule aimed at logs/ can't reach it).
#   Do NOT rotate this file with an EXTERNAL tool (logrotate/copytruncate)
#   while the server runs: it keeps one append handle open and never reopens,
#   so rename rotation silently drops later entries and copytruncate makes
#   `audit verify` cry tampering. Use MaxSizeBytes below for built-in,
#   chain-safe rotation instead, or rotate externally only while stopped.
#   Do NOT point two processes at one ledger (e.g. server + server2 in the
#   demo): there is no cross-process lock, so interleaved appends break the
#   chain and one process's tail repair can delete the other's entries. Give
#   each instance its own file.
# Fsync: flush each entry to disk before the request continues. Entries are
#   written on the request path, so audit volume tracks knock volume: this
#   costs a disk flush per access decision and serializes audit writes.
#   Pair it with Async below to keep the flush off the knock goroutine. Left
#   off in this sample so the first operator to enable auditing doesn't
#   silently inherit a per-knock flush on the UDP path; turn it on when
#   losing the last few entries to a crash is unacceptable.
# Async: move the disk write (and Fsync) onto a single background writer.
#   Log still computes seq + the hash chain synchronously, so ordering and
#   integrity are unchanged; only the write is deferred. If the writer falls
#   far enough behind that the queue (AsyncQueueSize, default 4096) fills,
#   entries are DROPPED and counted rather than blocking the knock — a
#   dropped entry is discarded whole so the chain stays contiguous. Off by
#   default (the synchronous write is the strongest "on disk before we
#   answered" guarantee).
# MaxSizeBytes: when > 0, roll the ledger to "<FilePath>.<seq>" once it would
#   grow past this many bytes and continue in a fresh file. The hash chain
#   spans the segments and `audit verify` walks them automatically, so this
#   is the safe way to bound ledger growth without downtime. 0 = single file
#   that grows forever (archive it out of band).
# MaxSegments: when > 0, keep at most this many "<FilePath>.<n>" files —
#   after each rotation the oldest are deleted. With MaxSizeBytes this bounds
#   total ledger disk to ~MaxSizeBytes*(MaxSegments+1). This matters because
#   NHP_OTP / NHP_REG requests are audited BEFORE the peer is validated, so a
#   party that knows the server's public key could otherwise fill the disk.
#   0 (default) keeps every segment — only use that if you archive segments
#   off-box. Deleting segments means `audit verify` can no longer start from
#   seq 1; it anchors on the first surviving entry and says so.
# SigningKeyBase64: optional base64 HMAC key binding the chain to a secret
#   the log file does not contain (must be >= 32 bytes; shorter is rejected
#   at startup). Generate one with:  head -c 32 /dev/urandom | base64
# FailClosed: what to do when the ledger can't be opened at startup.
#   false (default): fail SAFE — a corrupt/foreign file at FilePath is moved
#     aside to "<path>.corrupt-<timestamp>" and a fresh chain is started, so
#     the gateway keeps producing a trail (a lone junk byte can't silently
#     switch auditing off). Other failures leave auditing disabled with a
#     Critical log, but the server still serves.
#   true: fail CLOSED — any open failure aborts startup. Use when an
#     uninterrupted trail matters more than availability.
#   Either way this governs STARTUP only. A write that fails once the server
#     is running (disk full, file made unwritable) is logged but never blocks
#     the request; a sustained run escalates to a rate-limited Critical.
[Audit]
Enabled = false
FilePath = ""
Fsync = false
Async = false
AsyncQueueSize = 0
MaxSizeBytes = 0
MaxSegments = 0
SigningKeyBase64 = ""
FailClosed = false
