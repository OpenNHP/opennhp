# OpenNHP Architecture

The OpenNHP architecture follows the **NIST Zero Trust Architecture** standard with a modular design. Hover over components to learn more, or click **Show Flow** to see the protocol in action.

<ArchDiagram />

<!--
  VitePress-only page. Exists solely so tools/capture-arch.mjs can
  navigate to /arch-demo and screenshot the interactive diagram.
  The deployed docs site (Jekyll) never serves this page; see
  docs/index.md for the published version which embeds the
  captured GIF.
-->
